name: PR Approvals Github Action
on:
  pull_request_review:
    types: [edited, submitted]
jobs:
  run:
    name: Test if works
    runs-on: [ubuntu-latest]
    if: contains(github.event.pull_request.base.ref, 'pre-release') != true
    steps:
      - name: Echo
        run: |
          echo "Works"

